<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".ChatActivity"/>
        <activity android:name=".ChatSelectActivity" />
        <activity android:name=".ChatSecondActivity" />
        <activity android:name=".ChatResultActivity" />
        <activity android:name=".MedicineActivity" />
        <activity android:name=".MedicineResultActivity" />
        <activity android:name=".BasicQuestionActivity" />
        <activity android:name=".SecondQuestionActivity" />
        <activity android:name=".DietActivity" />
        <activity android:name=".DietResultActivity" />
        <activity android:name=".DietDiseaseResultActivity" />
        <activity android:name=".ProfileActivity" />
    </application>

</manifest>